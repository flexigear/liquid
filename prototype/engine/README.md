# Engine Placeholder

Put the first actual prototype project here after the engine is chosen.

Recommended convention:

- Unity: `prototype/unity/`
- Godot: `prototype/godot/`
- Custom renderer/simulation: `prototype/engine/` can become the working project root
